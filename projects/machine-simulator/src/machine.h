/* Wrapper header to allow src/ .c files to include "machine.h" while the canonical
   header is stored in ../include/. This preserves the original headers and avoids
   changing the build invocation used by the public readiness checker. */
#ifndef PROJECTS_MACHINE_SIMULATOR_SRC_MACHINE_H
#define PROJECTS_MACHINE_SIMULATOR_SRC_MACHINE_H

#include "../include/machine.h"

#endif /* PROJECTS_MACHINE_SIMULATOR_SRC_MACHINE_H */
