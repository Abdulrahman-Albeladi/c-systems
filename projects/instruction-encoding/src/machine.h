/* Wrapper header to forward to ../include/machine.h so the src file can compile
   when checked by the readiness script (which doesn't add -I). */
#ifndef PROJECTS_INSTRUCTION_ENCODING_SRC_MACHINE_H
#define PROJECTS_INSTRUCTION_ENCODING_SRC_MACHINE_H

#include "../include/machine.h"

#endif /* PROJECTS_INSTRUCTION_ENCODING_SRC_MACHINE_H */
